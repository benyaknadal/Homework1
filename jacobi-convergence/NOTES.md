إليك **تنسيقاً احترافياً ومنظماً لمحتوى "ملاحظات التنفيذ" دون أي تغيير في النص أو المعنى**، وجاهز لوضعه مباشرة في ملف `NOTES.md`:

---

# 📝 ملاحظات التنفيذ

## 📌 الدوال المستخدمة في MATLAB

---

## 1️⃣ `is_diagonally_dominant.m`

### 🎯 الغرض

فحص ما إذا كانت المصفوفة تحقق **السيطرة القطرية الصارمة**.

### ⚙ الخوارزمية

1. التحقق من أن المصفوفة **مربعة** ( (n \times n) ).
2. لكل صف (i):

   * حساب القيمة المطلقة للعنصر القطري ( |a_{ii}| )
   * حساب مجموع القيم المطلقة للعناصر الأخرى في نفس الصف
   * فحص الشرط:

[
|a_{ii}| > \sum_{j \ne i} |a_{ij}|
]

3. إرجاع `true` فقط إذا تحقق الشرط لجميع الصفوف.

### 🧰 الدوال المستخدمة

| الهدف                | الدالة   |
| -------------------- | -------- |
| معرفة أبعاد المصفوفة | `size()` |
| القيمة المطلقة       | `abs()`  |
| جمع العناصر          | `sum()`  |

### 💡 مثال استخدام

```matlab
A = [4 1 1; 1 5 1; 1 1 6];
result = is_diagonally_dominant(A);
% result = true
```

---

## 2️⃣ `jacobi_spectral_radius.m`

### 🎯 الغرض

بناء **مصفوفة التكرار** لطريقة جاكوبي وحساب **نصف قطر الطيف**.

### ⚙ الخوارزمية

1. استخراج المصفوفة القطرية:

```matlab
D = diag(diag(A));
```

2. التحقق من عدم وجود **أصفار في القطر**

3. استخراج الأجزاء المثلثية:

   * الجزء السفلي:

     ```matlab
     L = tril(A, -1);
     ```

   * الجزء العلوي:

     ```matlab
     U = triu(A, 1);
     ```

4. حساب مصفوفة التكرار:

[
B = D^{-1}(L + U)
]

5. حساب القيم الذاتية:

```matlab
e = eig(B);
```

6. إيجاد نصف قطر الطيف:

[
\rho = \max(|e|)
]

### 🧰 الدوال المستخدمة

| الهدف          | الدالة   |
| -------------- | -------- |
| استخراج القطر  | `diag()` |
| المثلث السفلي  | `tril()` |
| المثلث العلوي  | `triu()` |
| القيم الذاتية  | `eig()`  |
| القيمة المطلقة | `abs()`  |
| القيمة العظمى  | `max()`  |

### 💡 مثال استخدام

```matlab
A = [4 1 1; 1 5 1; 1 1 6];
[B, rho] = jacobi_spectral_radius(A);

fprintf('Spectral radius: %.4f\n', rho);

if rho < 1
    fprintf('الطريقة تتقارب!\n');
else
    fprintf('الطريقة لا تتقارب!\n');
end
```

---

## 🧠 نصائح برمجية

### ✔ يفضل استخدام `\` بدلاً من `inv()`

```matlab
% طريقة أفضل (أسرع وأدق عددياً)
B = D \ (L + U);

% طريقة أقل كفاءة
B = inv(D) * (L + U);
```

---

## 🛡 معالجة الأخطاء

الكود يتحقق من:

* المصفوفة **مربعة**
* **عدم وجود أصفار على القطر** (شرط مهم لطريقة جاكوبي)

---

## 🧪 أمثلة اختبار

### ✔ مثال 1: مصفوفة متقاربة

```matlab
A1 = [4 -1 0;
      -1 4 -1;
       0 -1 4];

is_diag_dom = is_diagonally_dominant(A1);
[B1, rho1] = jacobi_spectral_radius(A1);

fprintf('Diagonally dominant: %d\n', is_diag_dom);
fprintf('Spectral radius: %.4f\n', rho1);
```

---

### ✘ مثال 2: مصفوفة غير متقاربة

```matlab
A2 = [1 2 3;
      4 5 6;
      7 8 9];

is_diag_dom = is_diagonally_dominant(A2);
[B2, rho2] = jacobi_spectral_radius(A2);

fprintf('Diagonally dominant: %d\n', is_diag_dom);
fprintf('Spectral radius: %.4f\n', rho2);
```

---

## 📎 ملاحظات مهمة

* **السيطرة القطرية شرط كافٍ وليس ضرورياً**
  ⇒ قد تتقارب الطريقة حتى لو لم يتحقق الشرط.

* **نصف قطر الطيف هو المعيار النهائي للتقارب**
  ⇒ يجب أن يكون:

[
\rho(B) < 1
]

* **الدقة العددية**
  استخدم `\` بدلاً من `inv()` للحصول على نتائج أكثر ثباتاً.

* **الأداء**
  الاعتماد على دوال MATLAB المدمجة يضمن أداء أسرع من الحلقات اليدوية.

---

🔹 تم التنسيق
🔹 المحتوى بقي كما هو
🔹 جاهز للدمج في ملف مشروعك Markdown
